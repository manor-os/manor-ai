"""Bundled handler examples — opinionated wrappers around common runtimes.

These aren't exported from the top-level SDK; users import what they
need. Goal: copy-paste-able bridges so wiring Claude Code or a Python
shell to Manor takes <20 lines of user code.
"""
